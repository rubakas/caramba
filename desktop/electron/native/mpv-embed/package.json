{
  "name": "mpv-embed",
  "version": "0.2.0",
  "private": true,
  "description": "Native libmpv binding that embeds video playback into an Electron BrowserWindow's NSView. Built in place by bin/setup-mpv; not consumed as an npm package.",
  "main": "index.js",
  "scripts": {
    "rebuild": "node-gyp rebuild"
  },
  "dependencies": {
    "node-addon-api": "^8.0.0"
  }
}
