# Dependencies
node_modules

# Build output
dist-react
dist

# Storage (user data)
desktop/storage

# Bundled binaries (downloaded by bin/setup-ffmpeg, bin/setup-mpv)
desktop/vendor/ffmpeg*
desktop/vendor/vlc*
desktop/vendor/mpv*

# Maintainer-only build output (desktop/bin/build-libmpv)
desktop/dist

# Native module build output
desktop/electron/native/*/build
desktop/build

# OS
.DS_Store

# Env (Vite convention: *.local files hold secrets, never checked in)
.env.local
.env.*.local
*.env.local

# pnpm
pnpm-debug.log*

# Playwright
.playwright-mcp
tests/test-results
tests/playwright-report
tests/.playwright
