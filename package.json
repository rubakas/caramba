{
  "private": true,
  "version": "v1.3.6",
  "description": "Caramba — media center monorepo",
  "scripts": {
    "test:ui": "pnpm --filter @caramba/ui test"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "electron",
      "esbuild"
    ]
  }
}
