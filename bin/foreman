#!/usr/bin/env bash
# Ensure foreman is installed, then exec it with all passed arguments.
if ! command -v foreman &>/dev/null; then
  echo "Installing foreman..."
  gem install foreman
fi

exec foreman "$@"
